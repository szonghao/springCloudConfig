server:
  port: 8889
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1001/eureka/,http://localhost:1002/eureka/
spring:
  application:
    name: config-center
  cloud:
    config:
      server:
        git:
          uri: https://github.com/bashu-Alex/springCloudConfig/
          searchPaths: respo
      label: master
